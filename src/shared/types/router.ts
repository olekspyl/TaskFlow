export enum RouteNames {
  ROOT = 'Root',
  DASHBOARD = 'Dashboard',
  LOGIN = 'Login',
  NOT_FOUND = 'NotFound',
  USERS = 'Users',
  USER_DETAILS = 'User details',
}

export enum RouteLayouts {
  DEFAULT = 'default',
  AUTH = 'auth',
}
